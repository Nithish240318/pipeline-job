pipeline {
    agent any
    stages {
        stage('Addition') {
            steps {
                script {
                    def a = 3
                    def b = 4
                    def sum = a + b
                    echo "sum of ${a} and ${b} is ${sum}"
                }
            }
        }
    }
}
